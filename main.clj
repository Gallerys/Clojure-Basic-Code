;; This is a comment in Clojure. It's ignored by the Clojure interpreter and used for documentation.

;; Define a function to greet a person
(defn greet [name]
  (println (str "Hello, " name "!")))

;; Call the greet function with different names
(greet "Alice")
(greet "Bob")
